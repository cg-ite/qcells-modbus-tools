[project]
name = "qcells-modbus-tools"
version = "0.1.0"
description = "Some python tools for QCells Q.VOLT P(5-15)T Inverter (SolarX Mic 3), modbus, mqtt, Shelly 3empro, Marstek Venus 3 and Home Assistant"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "asyncio-mqtt>=0.16.2",
    "pymodbus>=3.11.4",
]
